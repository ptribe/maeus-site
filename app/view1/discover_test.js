'use strict';

describe('myApp.discover module', function() {

  beforeEach(module('myApp.discover'));

  describe('discover controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var discoverCtrl = $controller('DiscoverCtrl');
      expect(discoverCtrl).toBeDefined();
    }));

  });
});
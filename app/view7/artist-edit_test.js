'use strict';

describe('myApp.artist-edit module', function() {

  beforeEach(module('myApp.artist-edit'));

  describe('artist-edit controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view3Ctrl = $controller('ArtistEditCtrl');
      expect(view3Ctrl).toBeDefined();
    }));

  });
});
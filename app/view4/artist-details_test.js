'use strict';

describe('myApp.artist-details module', function() {

  beforeEach(module('myApp.artist-details'));

  describe('artist-details controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view3Ctrl = $controller('ArtistDetailsCtrl');
      expect(view3Ctrl).toBeDefined();
    }));

  });
});
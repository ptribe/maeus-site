'use strict';

describe('myApp.artist-dash module', function() {

  beforeEach(module('myApp.artist-dash'));

  describe('artist-dash controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view3Ctrl = $controller('ArtistDashCtrl');
      expect(view3Ctrl).toBeDefined();
    }));

  });
});